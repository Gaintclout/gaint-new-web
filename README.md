# Gaint New web

This is the local project for the Vite + React site. Repository initialized locally by an automated assistant.

Next steps:
- Create a GitHub repository and push the local `main` branch.
- Optionally enable GitHub Actions or add CI.